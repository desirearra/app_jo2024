/**
 * Affiche une page contenant un en-tête "Événements".
 *
 * Ce composant présente un conteneur stylisé avec un titre principal pour la section des événements.
 *
 * @returns Un élément JSX représentant la structure de la page des événements.
 */
export default function Events() {
  return (
    <div className="container mx-auto p-4">
      <h1 className="text-4xl font-bold mb-4">Événements</h1>
    </div>
  );
}
